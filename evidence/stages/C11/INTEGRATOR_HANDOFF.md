# C11 integrator handoff

C11/P13 is complete at clean implementation commit
`ff059edbc5bf10d61936a43b2b5bdfb05d69c133` on `rgsw/c11-p13`.
The final native executable is
`a7a2700556b553f7cfae298bbe681474fff9086cdab5a8c83d53b4869fd38825`.

The frozen kernel and global operator order remain unchanged. P13 fits O02;
do not create an operator-order v2 for C12 unless the export implementation
cannot fill the explicitly reserved O09 slot. If that happens, freeze the new
bytes and stop for independent review before implementation or outcomes.

For C12, first preregister the exact P14 transaction and O09 fit. Re-read the
adopted source for the exact interface contact-fraction definition, recipient
ledger, five `k_export` levels, and mass-to-mole carbon conversion. Do not infer
an unstated contact formula or value. Every enabled export case must have its
matched `k_export=0` control. C12 may not introduce P15 behavior.

Checkpoint schema v6 contains the complete P13 config and cumulative ledger.
Any C12 schema bump must requalify continuous, 1->1, 2-rank, and 2->1 restart,
plus fail-closed corruption before particle deserialization.

The P13 authority classification remains
`USER_ADOPTED_PROVISIONAL_PENDING_MENTOR_OVERRIDE`; no biological baseline,
mentor approval, or release-host claim was made. C10 and C11 reference-host
timings are not P15 all-mechanism timing estimates.
